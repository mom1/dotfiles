- defaults:
    link:
      create: true
      relink: true
      force: true
    clean:
      force: true

- clean: [
    '~',
]

- shell:
  - [~/.fzf/install]
  - [pip3 install -q --upgrade pip --user, Update pip3]
  - [pip install -q --upgrade pip --user, Update pip]
  - ['curl -sSL https://raw.githubusercontent.com/sdispater/poetry/master/get-poetry.py | python', Installing poetry]

- pip:
    file: requirements.txt
    binary: pip3
    user: true    # Like --user
    stdout: false # Print the execution stdout
    stderr: true  # Print the execution stderr

- link:
    ~/.shell: shell
    ~/.bash_profile: config/bash_profile
    ~/.bashrc: config/bashrc
    ~/.zshrc: config/zshrc
    ~/.hushlogin: config/hushlogin
    ~/.antigenrc: config/antigenrc
    ~/.antigen.zsh: antigen/antigen.zsh
    ~/.config/mc: config/mc/.
    ~/.fzf: fzf/.
    ~/antigen: antigen/.
    ~/.pyenv: pyenv/.

    # SSH:
    ~/.ssh/config: config/ssh_config

    # Git:
    ~/.gitconfig: config/gitconfig
    ~/.gitignore_global: config/gitignore

    # Editors:
    ~/.editorconfig: config/editorconfig
    ~/.nanorc: config/nanorc

